# 领域：<名称>

<!-- 本领域的压缩认知（AI 主笔、人确认）。是该领域"是什么"的唯一描述源；src/domains 只放代码。
保持精简：几句话 + 要点列表。术语须与 .human/glossary.md 一致。 -->

## 职责与边界

<!-- 一句话：本领域负责什么、不负责什么。 -->

## 核心概念

<!-- 聚合根 / 实体 / 值对象 / 领域服务。
标注聚合根即一致性 / 事务边界；跨聚合只用 ID 引用，不持有对方对象（见 .human/architecture.md）。
示例：
- 聚合根 Order（事务边界）：含 OrderLine（实体）、Money（VO）；引用 CustomerId（跨聚合 ID）
- 领域服务 PricingService：跨实体计价 -->

## 不变量 / 业务规则

<!-- 必须始终成立的规则（状态机、约束）。
示例：订单确认后不可修改订单行。 -->

## 领域协作约束（可选）

<!-- 仅描述抽象协作约束，不绑定具体技术实现。
示例：
- 对外仅暴露聚合语义，不泄漏基础设施细节
- 跨聚合协作由 feature 编排完成 -->

## 所属限界上下文

<!-- 见 .human/architecture.md 的限界上下文。示例：ordering -->

## 链接

<!-- 代码：src/domains/<name>/ -->
